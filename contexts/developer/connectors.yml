# Developer context — CLI + webhook for CI/CD events
connectors:
  - name: cli
    type: cli
    username: dev

  - name: webhook-ci
    type: webhook
    host: 0.0.0.0
    port: 8080
    secret: ${WEBHOOK_SECRET}
    path: /webhook
